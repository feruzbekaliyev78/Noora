# Local dev — Vite proxy sends /api to localhost:3000
VITE_API_URL=http://localhost:3000

# Production (Netlify) — set in Netlify env vars or api.js falls back to:
# https://noora-production.up.railway.app
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
